VITE_API_URL=http://localhost:4000/api
# Leave empty for local dev / docker-compose (defaults to `${VITE_API_URL}/uploads`). Set to the
# Supabase Storage public bucket URL when deploying on Cloudflare Pages — see
# docs/deploiement-gratuit.md.
VITE_UPLOADS_BASE_URL=
